muhammad qadeer khan 
etea id 06067
#include <iostream>
using namespace std;

int main() {
    int num;

    // Prompt user to enter an integer
    cout << "Enter an integer: ";
    cin >> num;

    // Use logical operators to check conditions
    if (num > 0 && num < 100) {
        cout << "The number is greater than 0 and less than 100." << endl;
    } else {
        cout << "The number does not meet both conditions (greater than 0 and less than 100)." << endl;
    }

    return 0;
}
/*this program display the number in between zero to hundred and cannot display the number greater than hundred and less than zero


